foreign:
  func emit(...) -> unit

func tests() -> unit:
  -- unannotated
  let a = 1
  let b = a
  let c = a + b
  let d = null::*int
  let e = d
  let f = "test"
  let g = if true: a else: b
  let h = true
  let i = || 0
  let j = || pass

  let k = if h:
    pass

  -- annotated
  let l: int = 1
  let m: int = l
  let n: *int = null

  -- evaluate
  unsafe:
    emit(a)
    emit(b)
    emit(c)
    emit(d)
    emit(null::*int)
    emit("hi")
    emit(123)
    emit(false)
    emit(i)
    emit(j)
    emit(l)
    emit(m)
    emit(n)
