foreign:
  func emit(...) -> unit

const A: int = 123

func tests() -> unit:
  const B: bool = true
  const C: bool = false
  const D: int = 1 + 2
  const E: int = D
  const F: int = E + D
  const G: int = F + A + E

  unsafe:
    emit(A)
    emit(B)
    emit(C)
    emit(D)
    emit(E)
    emit(F)
    emit(G)
